export type Feature = {
  title: string
  description: string
}

export type Testimonial = {
  name: string
  role: string
  content: string
}

export type Pricing = {
  name: string
  price: string
  features: string[]
}